import type { BookData } from './types';

/**
 * cache.ts — كاش جلسة بسيط (Map في الذاكرة فقط)
 *
 * لا Redis، لا قاعدة بيانات، لا تخزين على القرص — تماماً كما يشترط
 * مستند المتطلبات. يعيش هذا الكاش طوال عمر تبويب المتصفح فقط، ويُصفَّر
 * تلقائياً عند إعادة تحميل الصفحة. ملاحظة مقصودة: resetAll() في
 * IsbnProcessorApp لا يُفرّغ هذا الكاش عمداً — تماماً كسلوك النسخة
 * الأصلية — حتى تستفيد عمليات معالجة لاحقة (ملفات مختلفة تحتوي ISBN
 * مشترك) من نفس الجلسة.
 */
export const resultsCache = new Map<string, BookData>();
